import styled from "styled-components";
import ImgLocation from "../icons/Group.svg"

const LogoLocation = styled.img.attrs({
    src: ImgLocation,
}) `
 padding-left: 12px;
 
`;

export default LogoLocation;